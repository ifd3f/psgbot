name: psgbot
dependencies:
  - base == 4.*
  - containers
  - data-fix
  - random

executables:
  psgbot:
    main: Main.hs
    source-dirs:
      - src
      - app

tests:
  psgbot-spec:
    main: Spec.hs
    source-dirs:
      - src
      - test
    dependencies:
      - hspec == 2.*
    build-tools: hspec-discover

